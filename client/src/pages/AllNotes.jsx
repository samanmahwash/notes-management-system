import NotesListPage from '../components/NotesListPage';

const AllNotes = () => (
  <NotesListPage
    title="All Notes"
    description="Every note you've written, all in one place."
    emptyTitle="Your notebook is empty"
    emptyDescription="Write your first note — it only takes a moment."
  />
);

export default AllNotes;
